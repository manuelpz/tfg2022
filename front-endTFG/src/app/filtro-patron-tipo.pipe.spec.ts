import { FiltroPatronTipoPipe } from './filtro-patron-tipo.pipe';

describe('FiltroPatronTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPatronTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
