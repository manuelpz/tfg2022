import { FiltroPatronPipe } from './filtro-patron.pipe';

describe('FiltroPatronPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPatronPipe();
    expect(pipe).toBeTruthy();
  });
});
