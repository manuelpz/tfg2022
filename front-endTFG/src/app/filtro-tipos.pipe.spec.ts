import { FiltroTiposPipe } from './filtro-tipos.pipe';

describe('FiltroTiposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTiposPipe();
    expect(pipe).toBeTruthy();
  });
});
